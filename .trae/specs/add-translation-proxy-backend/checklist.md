- [x] 前端翻译请求默认已改为调用本地后端代理接口，而不是直连 Gemini
- [x] `GEMINI_API_KEY` 仅通过服务端环境变量读取，没有继续作为前端主配置项暴露
- [x] 后端提供了最小翻译代理接口，例如 `POST /api/translate`
- [x] 后端会校验空输入、超长输入或缺少必要字段的请求
- [x] 后端能把 Gemini 返回结果整理为前端当前使用的结构化字段
- [x] 缺失 Key、认证失败、限流、网络失败和内部异常都有明确且不泄露敏感信息的反馈
- [x] 当同时存在 `OPENROUTER_API_KEY` 和 `GEMINI_API_KEY` 时，后端会优先使用 OpenRouter
- [x] 页面健康检查可以显示当前代理已接入的上游提供方，例如 OpenRouter 或 Gemini
- [x] 页面不再要求用户在浏览器里手动输入 API Key 才能进行默认翻译
- [x] 本地预览方式可以同时运行前端页面和后端代理
- [x] 页面在本地运行时可以成功访问代理接口
